<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.stop_and_flight">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Stop_and_flight"
        android:usesCleartextTraffic="true"
        >

        <activity android:name=".kwon.LoginMethodActivity" />
        <activity android:name=".kwon.JoinSNSNickname" />
        <activity android:name=".kwon.IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".lee.AppGuide" />
        <activity android:name=".kwon.MainActivity" />
        <activity android:name=".kwon.select_signup_method" />
        <activity android:name=".kwon.JoinActivity" />
        <activity android:name=".kwon.LoginActivity" />
        <activity android:name=".kim.CalendarlistActivity" />
        <activity android:name=".kwon.InfoActivity"/>
        <activity android:name=".kwon.FilghtActivity">

            <intent-filter>
                <action android:name="android.intent.action.FLIGHTACTIVITY" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <receiver android:name=".kwon.AlarmReceiver">
            <intent-filter>
                <action android:name="android.intent.action.sec" />
            </intent-filter>
        </receiver>

        <service
            android:name=".kwon.Accessibility"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    <meta-data
        android:name="com.google.android.gms.ads.APPLICATION_ID"
        android:value="ca-app-pub-1142742075217394~1537550283"
        />

    </application>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />

</manifest>